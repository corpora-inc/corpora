from django.apps import AppConfig


class CorporaCommanderConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    # name = "Corpora Commander"
    name = "corpora_commander"
    verbose_name = "Corpora Commander"
